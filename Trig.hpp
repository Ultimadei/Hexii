#pragma once

#include <cmath> 

constexpr double PI = 3.1415927f;
constexpr double PI_HALVES = PI * (1.0 / 2.0);
constexpr double PI_THIRDS = PI * (1.0 / 3.0);
constexpr double PI_FOURTHS = PI * (1.0 / 4.0);
constexpr double PI_SIXTHS = PI * (1.0 / 6.0);
constexpr double DEG_TO_RAD_CONSTANT = PI / 180.0;
constexpr double SQRT3 = 1.7320508f;
constexpr double SQRT3_RECIPROCAL = 0.5773503f;
constexpr double SQRT3_HALVES = SQRT3 * 0.5f;